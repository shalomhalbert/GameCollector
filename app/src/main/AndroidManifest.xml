<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.gamecollector">

    <!--Theme changed to "NoActionBar" for using Toolbar as ActionBar-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_game_collector_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_game_collector_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="android.app.default_searchable"
            android:value=".AddCollectableSearchResultsActivity" />

        <!--SoftInput="adjustPan" ensures keyboard doesn't cover EditText-->
        <activity
            android:name=".activities.CollectionActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--SoftInput="adjustPan" ensures keyboard doesn't cover EditText-->
        <activity
            android:name=".activities.CollectableActivity"
            android:label="Add to collection"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:parentActivityName=".activities.CollectionActivity"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <provider
            android:name=".data.sqlite.CollectableProvider"
            android:authorities="com.example.android.gamecollector"
            android:exported="false"/>
    </application>

</manifest>